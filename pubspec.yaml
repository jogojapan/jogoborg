name: jogoborg
description: Borg backup management web interface
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  go_router: ^17.1.0
  provider: ^6.1.1
  flutter_secure_storage: ^10.0.0
  crypto: ^3.0.3
  js: ^0.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true