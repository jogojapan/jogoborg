# Jogoborg Local Testing Environment Configuration
# Copy this file to env.local and customize for your environment

# ============================================================================
# Web Interface Configuration
# ============================================================================
JOGOBORG_WEB_PORT=8080
JOGOBORG_WEB_USERNAME=testuser
JOGOBORG_WEB_PASSWORD=testpass123
JOGOBORG_GPG_PASSPHRASE=test_encryption_key_change_me
JOGOBORG_URL=http://localhost:8080

# Default Borg repository passphrase for local testing
# Can be overridden per-job in the web UI
BORG_PASSPHRASE=test_borg_passphrase_change_me

# ============================================================================
# Local Directory Paths
# ============================================================================
# These paths are relative to the local_test directory
# They mirror the container paths: /config, /borgspace, /log, /sourcespace

JOGOBORG_CONFIG_DIR=./config
JOGOBORG_BORGSPACE_DIR=./borgspace
JOGOBORG_LOG_DIR=./logs
JOGOBORG_SOURCESPACE_DIR=./sourcespace

# ============================================================================
# Optional: Database Credentials for Testing
# ============================================================================
# Uncomment and configure if you want to test database backup integration

# PostgreSQL
# JOGOBORG_TEST_POSTGRES_HOST=localhost
# JOGOBORG_TEST_POSTGRES_PORT=5432
# JOGOBORG_TEST_POSTGRES_USER=testuser
# JOGOBORG_TEST_POSTGRES_PASSWORD=testpass
# JOGOBORG_TEST_POSTGRES_DB=testdb

# MySQL/MariaDB
# JOGOBORG_TEST_MYSQL_HOST=localhost
# JOGOBORG_TEST_MYSQL_PORT=3306
# JOGOBORG_TEST_MYSQL_USER=testuser
# JOGOBORG_TEST_MYSQL_PASSWORD=testpass
# JOGOBORG_TEST_MYSQL_DB=testdb

# ============================================================================
# Optional: S3/MinIO Configuration for Testing
# ============================================================================
# Uncomment and configure if you want to test S3 synchronization

# JOGOBORG_TEST_S3_ENDPOINT=http://localhost:9000
# JOGOBORG_TEST_S3_ACCESS_KEY=minioadmin
# JOGOBORG_TEST_S3_SECRET_KEY=minioadmin
# JOGOBORG_TEST_S3_BUCKET=jogoborg-backups

# ============================================================================
# Optional: Notification Configuration for Testing
# ============================================================================
# Uncomment and configure if you want to test notifications

# SMTP Email
# JOGOBORG_TEST_SMTP_HOST=localhost
# JOGOBORG_TEST_SMTP_PORT=1025
# JOGOBORG_TEST_SMTP_FROM=jogoborg@localhost

# Gotify Webhook
# JOGOBORG_TEST_GOTIFY_URL=http://localhost:8081
# JOGOBORG_TEST_GOTIFY_TOKEN=test_token

# ============================================================================
# Logging Configuration
# ============================================================================
JOGOBORG_LOG_LEVEL=DEBUG
JOGOBORG_LOG_FORMAT="%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# ============================================================================
# Development Settings
# ============================================================================
# Set to 'true' to enable auto-reload of Python services on file changes
JOGOBORG_DEV_AUTO_RELOAD=false

# Set to 'true' to enable verbose output
JOGOBORG_DEV_VERBOSE=true
