# Jogoborg Environment Configuration
# Copy this file to .env and modify the values

# Web Interface Configuration (JOGOBORG_ prefix avoids conflicts with other containers)
# IMPORTANT: Quote passwords containing special characters ($, `, \, etc.) to prevent shell expansion
# Example: JOGOBORG_WEB_PASSWORD='my$ecure@Pass123' (use single quotes)
JOGOBORG_WEB_USERNAME=admin
JOGOBORG_WEB_PASSWORD='changeme_secure_password'
JOGOBORG_WEB_PORT=8080

# Encryption Configuration
# This passphrase is used to encrypt sensitive data like database credentials and API keys
# Also quote if it contains special characters
JOGOBORG_GPG_PASSPHRASE='changeme_encryption_key'

# Optional: Timezone
TZ=UTC

# Optional: Docker Compose overrides
COMPOSE_PROJECT_NAME=jogoborg

# Source Directory Examples (modify docker-compose.yml volumes section)
# SOURCE_DIR_1=/home/user/documents
# SOURCE_DIR_2=/var/www
# SOURCE_DIR_3=/etc

# Backup Storage Location (modify docker-compose.yml volumes section)
# BACKUP_STORAGE_PATH=/mnt/backup-drive

# Example Database Connection (for reference - configure in web interface)
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=myapp
# DB_USER=myuser
# DB_PASS=mypassword

# Example S3 Configuration (for reference - configure in web interface)
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_BUCKET=my-backup-bucket
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key

# Example SMTP Configuration (for reference - configure in web interface)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Example Gotify Configuration (for reference - configure in web interface)
# GOTIFY_URL=http://gotify:80/message
# GOTIFY_TOKEN=your-app-token